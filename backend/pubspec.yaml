name: for_server
description: Servidor independiente para partidas remotas de Foundations of Rome.
publish_to: 'none'

environment:
  sdk: ^3.10.3

dependencies:
  for_core:
    path: ../packages
  postgres: ^2.6.4
  sqlite3: ^2.9.3

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.15
